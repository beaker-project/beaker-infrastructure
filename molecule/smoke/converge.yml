---
- name: Smoke test - common playbook
  hosts: all
  vars:
    playbook_test_mode: true
  roles:
    - base
    - entropy
    - guest_virt_tools
    - selinux
    - openssh
    - admin_ssh_keys
    - unattended_updates
    - chrony
